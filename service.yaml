apiVersion: v1
kind: Service
metadata:
  name: ai-devops-demo
  labels:
    app: ai-devops-demo
spec:
  type: ClusterIP
  selector:
    app: ai-devops-demo
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
